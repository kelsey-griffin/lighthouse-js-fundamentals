{
  "dependencies": {
    "prompt-sync": "^4.1.5"
  }
}

//generate a random #
Math.floor(Math.random() * (max - min + 1)) + min;

//print if too large or too small
console.log("Guess a number:");
guess = prompt("> ");
//if not a number 
isNan()
//print # tries
  //same number many times = 1 try
  //make an array of all the numbers tried 
//